<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Idan Aharon , Itay Fridman</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <nav class="navbar navbar-expand-md fixed-top navbar-dark bg-dark">
        @Html.ActionLink("Football", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    @Html.ActionLink("Home", "Index", "Home", new { area = "" }, new { @class = "nav-link" })
                </li>
                <li class="nav-item">
                    @Html.ActionLink("Admin Page", "AdminInterface", "Admin", new { area = "" }, new { @class = "nav-link" })
                </li>
                <li class="nav-item">
                    @Html.ActionLink("Contact", "Contact", "Home", new { area = "" }, new { @class = "nav-link" })
                </li>
            </ul>

        </div>
        @if (User.Identity.IsAuthenticated)
        {
            @Html.ActionLink("Logout", "LogOut", "Login", new { area = "" }, new { @class = "btn btn-outline-danger my-2 my-sm-0" })
        }
        else
        {
            @Html.ActionLink("Login", "UserLogin", "Login", new { area = "" }, new { @class = "btn btn-outline-success my-2 my-sm-0" })
        }
    </nav>

    <div class="container body-content">
        <br/>
        @RenderBody()
        
        <footer class="fixed-bottom text-center">
            <hr />
            <p>&copy; @DateTime.Now.Year - Idan Aharon , Itay Fridman</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
